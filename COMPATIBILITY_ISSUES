* Issues with Cisco ASA

Some users are reporting that newer versonf of xl2tpd (1.310 onward) are
not able to connect to Cisco ASA.

The work around is to use x2ltpd 1.39 and disable use of kernel module
(comment out the directive OSFLAGS+= -DUSE_KERNEL -D in the Makefile)
